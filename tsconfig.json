{
    "compilerOptions": {
        "module": "commonjs",
        "target": "es2017",
        "outDir": "./build",
        "noImplicitAny": true
    },
    "exclude": [
        "node_modules",
        "build"
    ],
    "files": [
        // Main
        "sources/main.ts",

        // Routes

        // Controllers

        // Middleware

        // Utils
        "sources/utils/fetchTree.ts",
        "sources/utils/loadRouters.ts"
    ]
}
